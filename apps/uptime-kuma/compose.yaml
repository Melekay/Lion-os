services:
  app:
    image: louislam/uptime-kuma:1.23.16
    restart: unless-stopped
    ports:
      - "127.0.0.1:${LION_APP_PORT}:3001"
    volumes:
      - ${LION_APP_DATA}/data:/app/data
    security_opt:
      - no-new-privileges:true
